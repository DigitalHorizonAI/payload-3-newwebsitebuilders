# Database connection string
DATABASE_URI=postgres://postgres:postgres@localhost:5432/<your-db-name>

# Used to encrypt JWT tokens
PAYLOAD_SECRET=YOUR_SECRET_HERE

# Used to configure CORS, format links and more. No trailing slash
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Public address the blog is served from, as visitors and Google see it.
# newwebsite.builders is a static site on Netlify; its /blog pages are built
# from this CMS's /api/articles, so canonical URLs, Open Graph tags and the
# sitemap must use the public host, never this app's own Railway host.
# Leave unset for local development - it falls back to NEXT_PUBLIC_SERVER_URL.
NEXT_PUBLIC_SITE_URL=https://newwebsite.builders
